Shared via symbolic links: `../.claude/skills/` => `.gemini/skills/`, `.agents/skills/`

Note: There is a bug in Claude Code where it cannot read directories that are symbolic links.
Reference: https://github.com/anthropics/claude-code/issues/14836
